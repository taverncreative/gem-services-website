export function generateCanonical(path: string) {
  return `https://www.gemservices.uk${path}`
}

export function generateMetadata() {
  /**
   * Generates dynamic core metadata (Title, Description, Graph) for Next.js SEO optimization.
   */
  return {}
}
